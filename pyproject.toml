[project]
name = "es-map"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = ["graphviz"]

[project.scripts]
es-map = "es_map.main:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
